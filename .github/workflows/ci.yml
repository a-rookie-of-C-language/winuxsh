name: Niubash CI

on:
  push:
    branches:
      - master
      - "rewrite/**"
  pull_request:
    branches:
      - master
      - "rewrite/**"

env:
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN: stable

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4.2.2

      # rubash is a git branch dependency resolved from Cargo.lock; no sibling
      # checkout is needed here. (The old --recurse-submodules clone pulled
      # third_party/bash from git.savannah.gnu.org and timed out repeatedly.)

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1.11
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt

      - name: Update rubash to latest master
        run: cargo update -p rubash

      - name: cargo fmt --check (root package)
        run: cargo fmt --check -p niubash

      - name: cargo build
        run: cargo build --locked

      - name: cargo test (workspace)
        run: cargo test --workspace --locked


