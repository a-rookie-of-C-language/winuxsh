interface:
  display_name: "Winuxsh"
  short_description: "Windows-native Bash and coreutils"
  default_prompt: "Use $winuxsh to run Bash through Winuxsh from pwsh/Codex, use native Windows paths, discover WinuxCmd/WPM utilities, and verify behavior against the active binary."
policy:
  allow_implicit_invocation: true
