interface:
  display_name: "Winuxsh"
  short_description: "Windows-native Bash and coreutils"
  default_prompt: "Use $winuxsh as a native Winuxsh session: run commands directly, use Windows paths, discover WinuxCmd/WPM utilities, and treat permission-looking failures as possible escalation issues before changing code."
policy:
  allow_implicit_invocation: true
